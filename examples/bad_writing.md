# Example Document for ProseGuard

This is definately a document that contains problems.  its a quick demo of
what the linter can detect for you.

We should of finished the release yesterday, but their was a issue with the
build server. The report was written by the team, and it was basically very
long. In order to make the release, we need to move fast, the tests are
more easier now than before and he don't need any extra approvals. i am
confident we can ship tomorrow.

```python
# Code blocks are protected — "definately" below is NOT linted:
def definately_not_linted():
    return "teh teh teh"
```

Inline code like `recieve` is ignored as well, and links to
[the site](https://example.com/recieve) stay untouched.
